<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register | MiniGram</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 min-h-screen text-white">

    <%- include('partials/header') %>

    <div class="flex items-center justify-center min-h-[calc(100vh-80px)]">
        <div class="backdrop-blur-md bg-white/10 border border-white/10 p-10 rounded-2xl shadow-2xl w-[90%] max-w-lg mt-4">
            <h2 class="text-3xl font-semibold text-center mb-6 text-green-400">Create Account 🌟</h2>
            <p class="text-sm text-zinc-400 text-center mb-8">
                Join <span class="text-green-400 font-medium">MiniGram</span> and start sharing your thoughts!
            </p>

            <form action="/register" method="post" class="space-y-4">
                <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-700 focus:border-green-500 outline-none transition placeholder-zinc-500" type="text" placeholder="Full Name" name="name" required>
                <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-700 focus:border-green-500 outline-none transition placeholder-zinc-500" type="text" placeholder="Username" name="username" required>
                <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-700 focus:border-green-500 outline-none transition placeholder-zinc-500" type="email" placeholder="Email" name="email" required>
                <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-700 focus:border-green-500 outline-none transition placeholder-zinc-500" type="number" placeholder="Age" name="age" required>
                <input class="w-full px-4 py-3 rounded-md bg-transparent border-2 border-zinc-700 focus:border-green-500 outline-none transition placeholder-zinc-500" type="password" placeholder="Password" name="password" required>

                <button class="w-full py-3 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-md transition-all">Create Account</button>
            </form>

            <p class="text-center text-sm mt-6 text-zinc-400">
                Already have an account?
                <a href="/login" class="text-green-400 hover:underline">Login</a>
            </p>
        </div>
    </div>

</body>
</html>