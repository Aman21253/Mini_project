<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Post | MiniGram</title>
  <link rel="icon" href="/favicon.png" type="image/png">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen">

  <%- include('partials/header') %>

  <main class="flex items-center justify-center px-4 py-12">
    <div class="bg-zinc-800 w-full max-w-lg p-8 rounded-2xl shadow-md border border-zinc-700">
      <h2 class="text-2xl font-semibold text-center mb-6 text-blue-400">✏️ Edit Your Post</h2>

      <form method="post" action="/update/<%= post._id %>" class="space-y-4">
        <textarea
          name="content"
          placeholder="Update your post content..."
          class="w-full min-h-[130px] p-4 bg-zinc-900 border border-zinc-700 rounded-md resize-none outline-none text-white focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all"
        ><%= post.content %></textarea>

        <div class="flex justify-center">
          <input
            type="submit"
            value="Update Post"
            class="px-6 py-2 bg-blue-500 hover:bg-blue-600 text-white font-medium rounded-md transition duration-300 cursor-pointer shadow-md hover:shadow-blue-500/30"
          />
        </div>
      </form>

      <div class="text-center mt-6">
        <a href="/profile" class="text-zinc-400 hover:text-blue-400 text-sm transition duration-200">
          ← Back to Profile
        </a>
      </div>
    </div>
  </main>

</body>
</html>